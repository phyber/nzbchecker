Python script to check article completion on an NNTP server.

You'll need to install the argparse module to use this. Either install it from
your distros packaging system, or install it with easy_install.
$ easy_install --user argparse

Works for me, YMMV.
